$(document).ready(function() {
    deleteService = function(itemId) {
        // $.ajax({
        //     url: '/services/' + itemId,
        //     method: 'DELETE'
        // }).done(function(data) {
        //     console.log(data);
        // });
        console.log(itemId);
    };
});
